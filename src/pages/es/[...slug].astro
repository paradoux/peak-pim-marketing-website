---
import { localizedStaticPaths } from "../../i18n/static-paths";

export const prerender = true;
export function getStaticPaths() {
  return localizedStaticPaths("es");
}
const { englishPath } = Astro.props;
return Astro.rewrite(englishPath);
---
